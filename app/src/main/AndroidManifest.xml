<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permiso para consumir APIs -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permiso para verificar el estado de conexión a internet del dispositivo -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppMovilPFTError404"
        tools:targetApi="31">
        <activity
            android:name=".actividades.DetalleReclamoActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.ListadoReclamosActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.ListadoReclamosEstudianteActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.CrearReclamoActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>